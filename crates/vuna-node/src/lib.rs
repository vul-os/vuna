//! vuna-node — the daemon: roles, the crawl→extract→index→publish loop, and the kotva-core binding
//! for [`vuna_core::kotva`] seams (identity, Publisher, ContentAddresser). TODO(agent).
#![allow(dead_code)]
